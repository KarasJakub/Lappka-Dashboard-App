import styled from "styled-components"

export const DashboardDefaultRootWrapper = styled.section`
    width: 100%;
    height: 100%;
    background-color: ${({ theme }) => theme.colors.lightGray5};
`